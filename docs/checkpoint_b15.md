# B15 预注册检查点分析（R6 决策文档）

日期：2026-09-16 15:30（Day 2）；触发条件：各策略列完成度 >> 20%

## 检查点问题（预注册，spec T2.0）
若 frozen 列相对最强传统基线的增益中位数 < 2%（BH 校正后无任何格
显著），触发方案复审会议，重估 LoRA/full FT 列的必要性。

## 数据快照（ledger 193 行，formal 173）

| 任务 | frozen vs 最强基线（random） | 增益 |
|---|---|---|
| ncRNA | 0.82 (RiNALMo) vs 0.900 (LGBM) | **-0.08（负）** |
| m6A | 0.92 (RiNALMo) vs 0.532 | **+0.39（强正）** |
| SSP | 0.196 (RiNALMo) vs 0.047 | **+0.15（强正）** |

中位数（三任务 frozen 增益）：**+0.15 > 2% 阈值** → 不触发复审。

## 决策
1. **继续 E1 矩阵补齐**（LoRA/full 列必要性维持——任务粒度分化本身
   已是主结论，两列都是证据载体）；
2. frozen 列在 ncRNA 上不敌 k-mer LGBM（-0.08）但在 per-base 任务上
   大幅领先（+0.15/+0.39）——该分化本身就是 C1/C2 素材，不是
   "预训练无收益"信号；
3. m6A/SSP 的 frozen 增益方向一致性 3/3（B14 标记 ↓*/↑*，见
   c4_table.md）。

## 记录纪律
本文件即 R6 要求的"检查点执行记录"；无触发事件（增益中位数 +0.15
远超 2% 阈值）。下次检查点：矩阵 100% 或 Day 7，先到者。
